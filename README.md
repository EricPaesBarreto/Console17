# Console17
A psychological thriller from the comfort of your terminal.


### Setup
#### UV
Run `uv sync` to setup the venv, packages etc.
#### Dockers:
Build the contanier: `docker compose up --build`

After running the image access through your browser via local host: `127.0.0.1:8000`